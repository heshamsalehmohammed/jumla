import initialState from '../initialState';


const ordersReducer = (state = initialState.ordersState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};


export default ordersReducer;
